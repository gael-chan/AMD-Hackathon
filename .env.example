# AMD MI300X via AMD Developer Cloud (primary — activates automatically when both are set)
AMD_API_KEY=
AMD_MODEL_ENDPOINT=
# AMD_MODEL=meta-llama/Llama-3.1-70B-Instruct

# Fireworks AI (fallback)
FIREWORKS_API_KEY=
# FIREWORKS_MODEL=accounts/fireworks/models/gpt-oss-120b

# Fixed demo FX rate
GBP_USD_RATE=1.27
